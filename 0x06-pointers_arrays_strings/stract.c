#include "main.h"
#include <string.h>

/**
 * _stract -> this is a function stract
 * @dest: first param
 * @src: second param
 * Return: a string
 */
char *_stract(char *dest, char *src)
{int len = 0, i;

while (dest[len])
len++;
}
for (i = 0; src[i] != '\0'; i++)
{
dest[len] = '\0';
return (dest);
}
